export class Book {
  constructor(
    bid: number,
    bname: string,
    author: string,
    category: string,
    availability: string
  ) {}
  bid: number;
  bname: string;
  author: string;
  category: string;
  availability: string;
}
